rules_version = '2';

// DEVELOPMENT RULES - Use for testing only, not production!
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to all documents for development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}