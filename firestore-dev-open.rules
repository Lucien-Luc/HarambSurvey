rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT ONLY - OPEN ACCESS FOR TESTING
    // DO NOT USE IN PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}